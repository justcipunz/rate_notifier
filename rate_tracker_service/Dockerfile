# 1. Берем официальный образ Python
FROM python:3.10-slim

# 2. Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# 3. Копируем файл с зависимостями
# Это делается отдельно для эффективного кэширования слоев Docker
COPY requirements.txt .

# 4. Устанавливаем зависимости
# Эта команда выполнится внутри Linux-контейнера, где всё сработает
RUN pip install --no-cache-dir -r requirements.txt

# 5. Копируем весь код нашего приложения в контейнер
COPY ./app .

# 6. Команда, которая будет запускаться при старте контейнера
# Запускаем uvicorn, чтобы он был доступен извне контейнера
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
